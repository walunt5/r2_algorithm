frames:
  global_frame: map
  robot_frame: chassis_base_link

topics:
  cmd_vel: /cmd_vel

files:
  nav_goals_package: r2_nav_bringup
  nav_goals_relative_path: config/r2_nav_goals.yaml

control:
  rate_hz: 30.0
  default_timeout_sec: 8.0
  tf_timeout_sec: 0.1

strategy:
  mode: yaw_gate_xy_pid

  # yaw 小于这个值，允许从“只旋转”进入“只平移”
  yaw_enter_xy_threshold: 0.05

  # 平移过程中 yaw 大于这个值，退回“只旋转”
  yaw_exit_xy_threshold: 0.09

tolerance:
  x: 0.06
  y: 0.06
  yaw: 0.05
  stable_time_ms: 400

pid:
  x:
    kp: 0.45
    ki: 0.0
    kd: 0.02
    i_limit: 0.1

  y:
    kp: 0.45
    ki: 0.0
    kd: 0.02
    i_limit: 0.1

  yaw:
    kp: 1.2
    ki: 0.0
    kd: 0.05
    i_limit: 0.2

limits:
  max_vx: 0.6
  max_vy: 0.6
  max_wz: 0.4